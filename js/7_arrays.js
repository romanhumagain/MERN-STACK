const arr = ["Roman", 6, 5, true]

console.log(typeof arr[1])

// function inside the class is call methods()


// push() methods add the element to the last
// pop() methods remove the last element of the array

// shift() methods add the element at the first
// unshift() methods remove the element from the first